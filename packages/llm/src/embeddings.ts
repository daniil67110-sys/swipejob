/**
 * Embeddings client stub — calcul des vecteurs sémantiques
 * Utilisé pour le matching IA (Story 2.8)
 */
export async function getEmbedding(_text: string): Promise<number[]> {
  // Stub — sera implémenté avec Mistral embeddings en Story 2.8
  return [];
}
