/**
 * SwipeJob — Schéma Drizzle
 * Sera implémenté en Story 1.2+ avec les tables :
 * - users, profiles, offers, applications, swipes, matches, consents
 */

// Placeholder — sera complété avec les vraies tables en Story 1.2
export const schema = {} as const;
